const loadMore=document.querySelector(".community_load_more"),btnState=document.querySelector(".community_btn_state");function showContent(e){const t=document.querySelectorAll(".community_item");Array.from(t).filter((e,t)=>t>4).map(e=>e.classList.toggle("hide")),"Show More"===loadMore.innerHTML?(loadMore.innerHTML="Show Less",btnState.innerHTML="-"):(loadMore.innerHTML="Show More",btnState.innerHTML="+"),e.preventDefault()}loadMore&&btnState&&loadMore.addEventListener("click",showContent);